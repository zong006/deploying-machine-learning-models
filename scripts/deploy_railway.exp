#!/usr/bin/expect -f

# Access the environment variable directly
set railway_token $env(RAILWAY_TOKEN)

puts "RAILWAY_TOKEN is set to: $railway_token"
# spawn env RAILWAY_TOKEN=$railway_token railway link
# expect "? Select a project"
# send "house_api\r"
# expect "? Select an environment"
# send "production\r"
# expect eof
